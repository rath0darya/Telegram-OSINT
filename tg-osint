#!/data/data/com.termux/files/usr/bin/sh
set -eu
ROOT="$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)"
if [ -f "$ROOT/.venv/bin/python" ]; then
  exec "$ROOT/.venv/bin/python" -m tg_osint.cli "$@"
fi
exec python -m tg_osint.cli "$@"
