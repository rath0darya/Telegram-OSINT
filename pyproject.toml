[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "telegram-osint"
version = "0.2.0"
description = "Termux-native Telegram public-information OSINT toolkit"
requires-python = ">=3.10"

[project.scripts]
tg-osint = "tg_osint.cli:main"